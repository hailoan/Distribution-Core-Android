AUTOMATION: CONTINUE

# IMPLEMENT-PLAN — play-video-using-ffmpeg-api

## 1. Planning control

| Item | Status / value | Evidence / consequence |
| --- | --- | --- |
| Source design | Approved `SOLUTION-DESIGN.md`, first line `AUTOMATION: CONTINUE`, working-tree revision 2026-09-09 | Implements AC-1–AC-9 without redesign. |
| Outcome | CONTINUE | The concrete Kotlin, JNI, native decode, EGL, test, and verification surfaces resolve with no planning blocker. |
| Primary owner | `:videolib` | Public facade namespace `com.cii.videolib`; native target `libvideolib.so`. |
| Changed modules | `:videolib` only | `:app` is a verification consumer, not a production-code owner. No `:camera`, vendored FFmpeg, publication, or dependency changes. |
| Dependency closure | `:videolib` → `:app` through `app/build.gradle.kts:55`; external consumers unknown | Preserve source/binary/runtime behavior and validate the direct consumer plus packaged native artifact. `:benchmark` is excluded because no app package/startup contract changes. |
| Crossed contracts | Public Kotlin, Kotlin/JNI declarations and exports, native ownership/threading, FFmpeg decode/frame conversion, EGL/GLES presentation, CMake source linkage, ARM ABI/AAR packaging | Each receives a production task and a linked test or integration check. |
| Investigation | Standard planning pass: current 3,888-node graph first; five depth-1 symbol relationship queries; five focused source/config inventories. Graph edges around C++ ownership were validated against source. | Exact paths, existing symbols, consumers, tests, and collision keys are confirmed. Vendored headers/build outputs were not used as structural evidence. |
| Naming resolution | Public `PlaybackListener`, `PlaybackError`, `VideoPreview.play`, and `VideoPreview.stop`; native `VideoPlayback` in `video_playback.h/.cpp` | Adjacent Kotlin uses one public type per PascalCase file; native files use `snake_case` and classes use PascalCase. These names concretize the approved semantic contracts without adding an architectural boundary. |
| Public start contract | `VideoPreview.play(path: String, listener: PlaybackListener): Boolean`; listener exposes completion and `PlaybackError`; `stop()` is idempotent | Implements the design's immediate accepted/rejected result and asynchronous terminal result. Exact callback names and enum members are fixed in CH-01–CH-03 below. |
| Assumptions | A small synthetic H.264/MP4 video-only fixture may be committed under `src/androidTest/assets`; its provenance/license and checksum will be documented. Android instrumentation is the narrowest existing test level for JNI/native loading. | No production codec guarantee is inferred from the fixture. |
| Unresolved planning inputs | None | Android development can execute every task without choosing architecture, paths, symbols, callback semantics, or verification scope. |
| Protected operations | Not part of plan | No commit, push, signing, publication, upload, distribution, FFmpeg rebuild, dependency upgrade, or vendored archive edit. |

## 2. Change-surface inventory

`Design-Ref: none — requirement/code-driven` applies to every change because the source design contains no UI design reference.

| Change-ID | Existing / new | Action | Exact path | Symbol / resource / config key | Design-Ref(s) | Responsibility | Evidence / design decision | Shared / collision key |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| CH-01 | existing | extend | `videolib/src/main/java/com/cii/videolib/VideoPreview.kt` | `VideoPreview`; add `play(path, listener): Boolean`, `stop()`, private `nativePlay`, `nativeStop`, and attempt-aware native completion/error receivers; extend `detachSurface`/`release` ordering | none — requirement/code-driven | Additive public control surface, listener ownership, main-thread dispatch, stale-event suppression, and stop/release barriers | Existing opaque handle and single-owner-thread API at `VideoPreview.kt:19-25`; AC-1–AC-7, AC-9 | `public-video-preview-contract` |
| CH-02 | new | create | `videolib/src/main/java/com/cii/videolib/PlaybackListener.kt` | public `PlaybackListener`; `onPlaybackCompleted()` and `onPlaybackError(error: PlaybackError)` | none — requirement/code-driven | Stable terminal notification contract | SOLUTION-DESIGN terminal-notification and error contracts | `public-video-preview-contract` |
| CH-03 | new | create | `videolib/src/main/java/com/cii/videolib/PlaybackError.kt` | public enum `PlaybackError`; `INPUT_OPEN`, `UNSUPPORTED_VIDEO`, `DECODE`, `RENDER` | none — requirement/code-driven | Stable semantic failures without exposing numeric FFmpeg/EGL details | SOLUTION-DESIGN §6 error contract | `public-video-preview-contract` |
| CH-04 | existing | extend | `videolib/src/main/cpp/preview_renderer.h` | `PreviewRenderer::pushFrame`; add explicit presentation success result and read-only readiness query needed by coordinator | none — requirement/code-driven | Report whether the synchronous RGBA presentation succeeded while preserving renderer ownership | Current `void pushFrame` and state at `preview_renderer.h:40-51`; AC-4, AC-8 | `preview-renderer-contract` |
| CH-05 | existing | extend | `videolib/src/main/cpp/preview_renderer.cpp` | `PreviewRenderer::pushFrame`; EGL current/draw/swap result propagation | none — requirement/code-driven | Convert EGL/swap failures into a render result without moving GL calls off the renderer thread | Current synchronous executor path at `preview_renderer.cpp:102-120`; AC-4, AC-8 | `preview-renderer-contract` |
| CH-06 | new | create | `videolib/src/main/cpp/video_playback.h` | `VideoPlayback`, `PlaybackState`, `PlaybackErrorCode`, terminal callback contract | none — requirement/code-driven | Own one renderer, one current attempt, native state arbitration, worker lifetime, and stop-before-release semantics | Approved playback coordinator and dedicated worker | `native-playback-core` |
| CH-07 | new | create | `videolib/src/main/cpp/video_playback.cpp` | `VideoPlayback` lifecycle plus local-input demux/decode/drain/timing/swscale path | none — requirement/code-driven | Open/select/decode video, pace frames, convert to RGBA, synchronously present, arbitrate EOS/error/cancellation, and release every native resource | FFmpeg 7.1 archives and renderer seam already exist; AC-1–AC-8 | `native-playback-core` |
| CH-08 | existing | extend/wire | `videolib/src/main/cpp/videolib.cpp` | replace `asRenderer` handle ownership with `asPlayback`; preserve existing exports; add `JNI_OnLoad`, `Java_com_cii_videolib_VideoPreview_nativePlay`, `..._nativeStop`, and attempt-aware callback bridge | none — requirement/code-driven | Keep Kotlin/JNI names exact, copy/release strings, own JVM/global references, attach worker threads safely, and delegate old preview operations through `VideoPlayback` | Current 1:1 exports and `PreviewRenderer*` handle at `videolib.cpp:37-44`; AC-1–AC-9 | `jni-video-preview-boundary` |
| CH-09 | existing | wire | `videolib/src/main/cpp/CMakeLists.txt` | `${CMAKE_PROJECT_NAME}` source list; add `video_playback.cpp` only | none — requirement/code-driven | Compile the coordinator into existing `libvideolib.so` without changing FFmpeg targets, ABIs, toolchain, or link order | Current shared target at `CMakeLists.txt:30-36` | `videolib-native-target` |
| CH-10 | existing | extend | `videolib/src/androidTest/java/com/cii/videolib/PreviewBindingTest.kt` | existing JNI/load/idempotence regression tests plus new play/stop export resolution after release/no surface | none — requirement/code-driven | Protect all pre-existing preview and `NativeLib` contracts during handle migration | Only confirmed current `VideoPreview` test owner | `preview-binding-tests` |
| CH-11 | new | create | `videolib/src/androidTest/java/com/cii/videolib/PlaybackContractTest.kt` | public validation, rejection, enum/listener, idempotent cancellation, and main-thread callback contract | none — requirement/code-driven | Instrumented contract coverage that does not assert visible pixels | Existing AndroidJUnit4 convention in `PreviewBindingTest.kt` | `playback-contract-tests` |
| CH-12 | new | create | `videolib/src/androidTest/java/com/cii/videolib/PlaybackLifecycleTest.kt` | attached-surface playback lifecycle, EOS, error, overlap, stop, surface loss, and callback race coverage | none — requirement/code-driven | Device-level JNI/native lifecycle and terminal arbitration coverage | Runtime behavior cannot be proven by compile | `playback-lifecycle-tests` |
| CH-13 | new | create | `videolib/src/androidTest/assets/playback_short.mp4` | short synthetic H.264/MP4 video-only fixture with distinct first/final frames and valid timestamps | none — requirement/code-driven | Deterministic supported local-path input copied to test-owned readable storage | FR-1/FR-3 device fixture | `playback-test-assets` |
| CH-14 | new | create | `videolib/src/androidTest/assets/README.md` | fixture provenance, synthetic-content/license statement, codec/container/timing description, generation recipe, and checksum | none — requirement/code-driven | Make the binary fixture reproducible and reviewable without creating a public codec promise | Test-fixture assumption | `playback-test-assets` |
| CH-15 | generated — do not edit | verify output | `videolib/build/outputs/aar/videolib-debug.aar` and ABI-specific `videolib/build/intermediates/**/libvideolib.so` | AAR native entries, ELF dependencies/symbols, 16 KB LOAD alignment | none — requirement/code-driven | Integration evidence only; authoritative inputs are CH-08/CH-09 and existing Gradle/archives | Native/ABI compatibility obligation | `generated-native-artifacts` |

No change is planned to `videolib/build.gradle.kts`, `app/**`, `settings.gradle.kts`, `consumer-rules.pro`, `videolib/src/main/cpp/ffmpeg/**`, or `videolib/ffmpeg-build/**`.

## 3. Work-item backlog

| FR-ID | SC-ID | AC-ID | Work-ID / Story-ID | Outcome | Module / screen | Depends on |
| --- | --- | --- | --- | --- | --- | --- |
| FR-1–FR-4 | SC-1–SC-4 | AC-7–AC-9 | W-0 / shared contract foundation | Add the stable public event model, renderer result seam, native owner, and JNI agreement required by all four stories without changing existing behavior. | `:videolib`; no screen | — |
| FR-1 | SC-1 | AC-1, AC-5, AC-6, AC-8, AC-9 | W-1 / ST-1 | An accepted readable local file continuously presents timestamp-ordered FFmpeg-decoded frames through the existing GLES surface; invalid lifecycle/overlap requests reject. | `:videolib`; host-supplied surface | W-0 |
| FR-2 | SC-2 | AC-2, AC-7, AC-9 | W-2 / ST-2 | Stop is idempotent and returns as a barrier against later frames and callbacks while preserving reuse of an attached surface. | `:videolib`; no screen | W-1 |
| FR-3 | SC-3 | AC-3, AC-7, AC-9 | W-3 / ST-3 | Natural decoder-drained EOS presents the final frame, does not loop, and produces exactly one main-thread completion event. | `:videolib`; host-supplied surface | W-1, W-2 |
| FR-4 | SC-4 | AC-4, AC-5, AC-7, AC-8, AC-9 | W-4 / ST-4 | Accepted open/decode/conversion/render failures stop safely and produce exactly one stable main-thread error; rejected requests produce none. | `:videolib`; host-supplied surface | W-1, W-2, W-3 |
| FR-1–FR-4 | SC-1–SC-4 | AC-1–AC-9 | W-5 / integration closure | Prove library/consumer compilation, native packaging/alignment, JNI lifecycle, and observable playback on supported ABIs without publishing. | `:videolib` producer; `:app` consumer | W-1–W-4 |

## 4. Task backlog

| Task-ID | Work-ID / Story-ID | Owning module | Owner stage | Objective | Change-IDs / exact path-symbol scope | Design-Ref(s) | Preconditions / inputs | Invariants | Done condition | Verification / Test-ID / Check-ID | Depends on | Collision key |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DEV-01 | W-0 | `:videolib` | android-dev | Establish the additive Kotlin playback contract and attempt-aware main-thread event gate. | CH-01 `VideoPreview`; CH-02 `PlaybackListener`; CH-03 `PlaybackError` | none — requirement/code-driven | Approved semantic start/terminal contracts | Existing methods/signatures remain; public calls retain single-owner-thread rule; rejected play stores no listener; stop/detach/release invalidate pending main-queue delivery; enum is limited to four approved categories | Kotlin source exposes the exact planned API/KDoc, maps internal native codes exhaustively, and cannot deliver a stale or post-stop/release event | TEST-01, TEST-02, CHECK-01 | — | `public-video-preview-contract` |
| DEV-02 | W-0 | `:videolib` | android-dev | Make synchronous frame presentation return a trustworthy render result. | CH-04/CH-05 `PreviewRenderer::pushFrame`, readiness/result handling | none — requirement/code-driven | Current render-thread executor and EGL lifecycle | Every EGL/GLES call remains on `videolib_render`; no FFmpeg dependency enters renderer; existing caller-pushed frame semantics remain; swap/current-context failure is observable; GL teardown stays context-current | Native callers can distinguish presented versus unavailable/failed without changing full-surface RGBA rendering or resource ownership | TEST-01, TEST-03, CHECK-01 | — | `preview-renderer-contract` |
| DEV-03 | W-1 / ST-1 | `:videolib` | android-dev | Add the FFmpeg video-only local-file decode, timestamp pacing, bounded ownership, RGBA conversion, and renderer integration core. | CH-06/CH-07 `VideoPlayback` happy path and internal outcomes; CH-09 native target source list | none — requirement/code-driven | DEV-02 result seam; bundled FFmpeg 7.1 headers/static archives | Decode/open/timing never run on caller or GL thread; audio ignored; only file input used; one worker/attempt; queues/memory bounded; frames ordered; late intermediate frames may drop; first/final not dropped solely for lateness; swscale/frame storage worker-owned and valid through synchronous render; delayed decoder frames drain at EOS; no vendored/build-profile change | New source compiles in the existing target and can open/select/decode/pace/convert/present a supported video to internal completion/error outcomes with deterministic cleanup | TEST-03, CHECK-01, CHECK-03, CHECK-04 | DEV-02 | `native-playback-core`, `videolib-native-target` |
| DEV-04 | W-2 / ST-2 | `:videolib` | android-dev | Complete native stop, detach, reattach, and destruction arbitration as a no-post-return barrier. | CH-06/CH-07 `VideoPlayback` state/cancellation/worker teardown | none — requirement/code-driven | DEV-03 owns the decode worker and renderer | One serialized state owner chooses terminal winner; stop is safe in Idle/Starting/Playing/Stopping/terminal states; no lifecycle mutex is held while blocking on renderer work or joining worker; detach cancels before EGL teardown; destruction cancels worker before renderer/member destruction; worker never joins itself | Repeated/racing stop, surface loss, EOS, error, and release converge without deadlock/use-after-free and cannot submit a frame or terminal event after the applicable barrier | TEST-02, TEST-03, CHECK-04 | DEV-03 | `native-playback-core` |
| DEV-05 | W-0, W-3 / ST-3, W-4 / ST-4 | `:videolib` | android-dev | Wire the public facade to `VideoPlayback`, preserve all old exports, and deliver only the winning completion/error through safe JNI references. | CH-08 all `VideoPreview` JNI exports, `JNI_OnLoad`, handle conversion, callback bridge; CH-01 private native declarations/receivers | none — requirement/code-driven | DEV-01 public types/gate; DEV-03/DEV-04 complete native state contract | Hand-mangled names and Kotlin types match exactly; native handle now owns `VideoPlayback`; old surface/frame/pattern/destroy exports delegate unchanged; path chars/local refs/global refs released exactly once; JVM cached through `JNI_OnLoad`; no `JNIEnv*` crosses threads; worker attaches/detaches only as required; attempts carry IDs; callback invocation occurs without native lifecycle locks; stop/release suppress stale queued Kotlin delivery; logs do not expose full input path | Accepted play returns true and installs exactly one attempt; rejection returns false; stop delegates barrier; final frame precedes one completion; each approved failure maps to one `PlaybackError`; old APIs and `NativeLib` exports still resolve | TEST-01, TEST-02, TEST-03, CHECK-01, CHECK-04 | DEV-01, DEV-03, DEV-04 | `jni-video-preview-boundary`, `public-video-preview-contract` |
| TEST-01 | W-0 | `:videolib` | testing | Protect JNI symbol agreement, handle migration, and all existing preview/linkage behavior. | CH-10 `PreviewBindingTest` | none — requirement/code-driven | DEV-05 complete JNI surface | Do not claim rendering; keep direct-buffer/invalid-input and double-release coverage; exercise new no-surface play/stop exports without needing EGL | Existing `VideoPreview` and `NativeLib` contracts plus new exports load, resolve, reject/no-op safely, and release idempotently | TEST-01; authored then compiled by CHECK-01 and executed by CHECK-04 | DEV-05 | `preview-binding-tests` |
| TEST-02 | W-2 / ST-2, W-4 / ST-4 | `:videolib` | testing | Cover public acceptance/rejection, semantic error mapping, main-thread delivery, idempotent stop/release, overlap rejection, and stale-callback suppression. | CH-11 `PlaybackContractTest` | none — requirement/code-driven | DEV-05; test-owned temporary readable files and a valid attached test surface where required | Assertions target public semantics, not numeric native codes or internal state names; callbacks are bounded with deterministic synchronization; cleanup always stops/releases | AC-2, AC-4, AC-5, AC-7, AC-9 and the Idle/Starting/Playing/Stopping/Failed/Released transitions have explicit instrumented coverage | TEST-02; authored then compiled by CHECK-01 and executed by CHECK-04 | DEV-05 | `playback-contract-tests` |
| TEST-03 | W-1 / ST-1, W-3 / ST-3, W-4 / ST-4 | `:videolib` | testing | Cover real bundled-FFmpeg decode through EGL, EOS/final-frame ordering, stop/surface races, and reproducible fixture ownership. | CH-12 `PlaybackLifecycleTest`; CH-13 fixture; CH-14 fixture documentation | none — requirement/code-driven | DEV-05; synthetic fixture; supported EGL device | Fixture is synthetic/licensable, short, video-only, timestamped, and documented; test copies asset to a readable local filesystem path; callbacks have time bounds; no codec matrix beyond fixture is asserted; surface/native resources always release | AC-1, AC-3, AC-4, AC-6–AC-9 have device-level lifecycle evidence, including completion after final presentation and no completion after stop/surface-loss winner | TEST-03; authored then compiled by CHECK-01 and executed by CHECK-04; visible-frame aspects additionally CHECK-05 | DEV-05 | `playback-lifecycle-tests`, `playback-test-assets` |
| INT-01 | W-5 | `:videolib` | integration-testing | Compile the changed library and all Android test sources through Kotlin/JNI/CMake. | CH-01–CH-14; inspect only CH-15 outputs | none — requirement/code-driven | DEV-01–DEV-05 and TEST-01–TEST-03 complete | No repair outside planned changes; preserve both ABI/toolchain/link settings | Commands finish successfully and results are recorded without claiming runtime behavior | CHECK-01 | TEST-01, TEST-02, TEST-03 | `gradle-videolib-build` |
| INT-02 | W-5 | `:videolib` | integration-testing | Inspect AAR/ELF output for native ABI, symbols/dependencies, and page alignment. | CH-15 generated outputs only; authoritative CH-08/CH-09 | none — requirement/code-driven | Successful CHECK-01 | Never edit generated artifacts; require both configured ARM entries and 16 KB-compatible LOAD alignment; confirm playback/legacy JNI exports and no unexpected dynamic FFmpeg `.so` packaging | Package report proves expected `libvideolib.so` entries and native contract for both ABIs | CHECK-02 | INT-01 | `generated-native-artifacts` |
| INT-03 | W-5 | `:app` consumer | integration-testing | Compile/package the direct in-repository consumer against the additive public library. | Read-only `app/build.gradle.kts:55`; generated app outputs | none — requirement/code-driven | Successful CHECK-01 | No app source modification and no publication | `:app` debug assembly succeeds with the changed library and native merge | CHECK-03 | INT-01 | `gradle-app-build` |
| INT-04 | W-5 | `:videolib` | integration-testing | Execute the instrumented contract/lifecycle suite on each available packaged ABI. | TEST-01–TEST-03; generated installed APKs | none — requirement/code-driven | CHECK-01; supported `arm64-v8a` and `armeabi-v7a` devices/emulators available | Record ABI/device/API and distinguish unavailable ABI from failure; no network or external media dependency | All instrumented tests pass per supported ABI, or unavailable ABI is explicitly recorded as environment-dependent rather than claimed | CHECK-04 | INT-01, INT-02 | `device-videolib-tests` |
| INT-05 | W-5 | `:videolib` | integration-testing | Validate observable GLES playback and final-frame retention that compilation/callback assertions cannot prove. | Public API from CH-01–CH-03 and CH-13 fixture; no source change | none — requirement/code-driven | CHECK-04 on a supported visible-surface device | Video only; current full-surface/coded-orientation policy; no audio/seek/pause/loop/rotation/SAR claims | Recorded device evidence shows continuous ordered frames, explicit stop, no loop, last frame retained after EOS while surface remains, and recovery with a later play | CHECK-05 | INT-04 | `device-visual-playback` |

## 5. Dependency map (DAG)

### Typed edges

| From | To | Edge type | Reason |
| --- | --- | --- | --- |
| DEV-02 | DEV-03 | contract | Playback consumes the renderer's explicit synchronous result/readiness seam. |
| DEV-03 | DEV-04 | behavior + ownership serialization | Stop/release arbitration owns the same `VideoPlayback` files after decode/worker ownership exists. |
| DEV-01 | DEV-05 | contract + ownership serialization | JNI names/types and callback bridge must match the established Kotlin API and private receivers. |
| DEV-03 | DEV-05 | wiring | JNI handle/export wiring requires the concrete `VideoPlayback` owner. |
| DEV-04 | DEV-05 | behavior | JNI detach/stop/destroy delegation requires the finalized cancellation barrier. |
| DEV-05 | TEST-01 | test | Binding regression tests require final exports/handle migration. |
| DEV-05 | TEST-02 | test | Public contract tests require final Kotlin/JNI behavior. |
| DEV-05 | TEST-03 | test | Device lifecycle tests require complete native decode/callback behavior. |
| TEST-01, TEST-02, TEST-03 | INT-01 | test | Test sources and fixtures must exist before compilation. |
| INT-01 | INT-02 | build artifact | AAR/ELF inspection requires generated outputs. |
| INT-01 | INT-03 | contract | Consumer verification follows a successful producer build. |
| INT-01, INT-02 | INT-04 | native/ABI | Device execution requires compiled tests and verified packages. |
| INT-04 | INT-05 | behavior | Visual validation follows a passing runtime lifecycle baseline. |

DEV-01 and DEV-02 have no dependency and have disjoint paths/collision keys. DEV-03 does not depend on DEV-01 because its terminal callback is a native semantic function contract; DEV-05 performs the language-boundary wiring.

Cycle check: pass. A topological order is `DEV-01 || DEV-02` → `DEV-03` → `DEV-04` → `DEV-05` → `TEST-01 || TEST-02 || TEST-03` → `INT-01` → `INT-02` → `INT-03` → `INT-04` → `INT-05`.

## 6. Execution waves

| Wave | Tasks | Concurrency / serialization rule | Entry condition |
| --- | --- | --- | --- |
| 1 | DEV-01, DEV-02 | May run concurrently: Kotlin public files versus C++ renderer files are disjoint. | Approved plan. |
| 2 | DEV-03 | Serialized after DEV-02: owns new playback core and CMake source-list collision. | Renderer result contract complete. |
| 3 | DEV-04 | Serialized with DEV-03 on `video_playback.h/.cpp`. | Decode/worker ownership complete. |
| 4 | DEV-05 | Serialized after DEV-01/DEV-04: collides with `VideoPreview.kt` and establishes shared JNI boundary. | Both language-side contracts stable. |
| 5 | TEST-01, TEST-02, TEST-03 | May run concurrently: existing binding test, new contract test, and lifecycle test/assets use disjoint files. | Final production surface complete. |
| 6 | INT-01 | Serialized Gradle producer/test compilation to avoid shared build-output contention. | All test source complete. |
| 7 | INT-02 | Read-only generated package inspection. | Producer build succeeds. |
| 8 | INT-03 | Serialized Gradle consumer build because it can rebuild/read the same `:videolib` outputs. | Producer build succeeds; package report available. |
| 9 | INT-04 | Device execution, one ABI target at a time with environment recorded. | Compile/package/consumer gates pass. |
| 10 | INT-05 | Visible-surface device observation. | Runtime automated baseline passes. |

No sprint plan is included because sprint packing, capacity, estimates, and priority were not requested.

## 7. Test scope and verification matrix

### Test scope

| Test-ID | AC-ID / risk | Level | Target component / contract | Behavior / transition / error scope | Fake / fixture boundary | Production Task-ID | Depends on | Execution expectation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TEST-01 | AC-5, AC-9; JNI symbol/handle regression | Android instrumentation binding | `VideoPreview`, `NativeLib`, all old/new JNI exports | Construct/load; no-surface rejection; push/pattern/detach guards; stop/release idempotence; legacy FFmpeg version/string exports | No surface or media; direct buffer only where existing contract needs it | DEV-01, DEV-05 | DEV-05 | Authored by testing; compiled by CHECK-01; runnable only on Android runtime via CHECK-04. |
| TEST-02 | AC-2, AC-4, AC-5, AC-7, AC-9; stale callback and terminal race | Android instrumentation contract | Public facade/listener plus JNI terminal bridge | Empty/no-surface reject; accepted invalid/corrupt input error category; overlap reject; repeated stop; stop/release callback suppression; callback main-thread and at-most-once | Test-owned temporary paths/files; lightweight valid surface only for accepted attempts | DEV-01, DEV-04, DEV-05 | DEV-05 | Authored by testing; compiled by CHECK-01; device/environment-dependent execution in CHECK-04. |
| TEST-03 | AC-1, AC-3, AC-4, AC-6–AC-9; decode/frame/EGL/lifecycle risk | Android instrumentation native integration | Bundled FFmpeg → `VideoPlayback` → `PreviewRenderer` → terminal bridge | Supported local decode; timestamp-forward playback; decoder drain; final presentation before one completion; stop and surface-loss winners; later-play recovery; no post-barrier event | CH-13 synthetic H.264/MP4 copied to app-private local path; attached test surface; corrupt temporary input | DEV-02–DEV-05 | DEV-05 | Authored by testing; compiled by CHECK-01; run on each available ABI in CHECK-04; visual claims reserved for CHECK-05. |

### Module integration matrix

| Check-ID | Changed module | Affected consumer / external contract | Boundary | Command or device/manual check | Why required | Owner integration-testing task |
| --- | --- | --- | --- | --- | --- | --- |
| CHECK-01 | `:videolib` | Public/native library and test sources | Kotlin/JNI/CMake/FFmpeg/EGL compile-link | `./gradlew :videolib:assembleDebug :videolib:assembleDebugAndroidTest` | Smallest build proving planned Kotlin/native/test sources compile and static archives link for configured ABIs | INT-01 |
| CHECK-02 | `:videolib` | External AAR consumers | Native/ABI/package | Inspect `videolib-debug.aar` entries and both ABI `libvideolib.so` files with archive listing plus NDK `llvm-readelf`/`llvm-nm`: two configured ARM ABIs, legacy/new JNI exports, expected native dependencies, and 16 KB-compatible LOAD alignment | Compile alone does not prove packaged ABI contents, exported names, or ELF alignment | INT-02 |
| CHECK-03 | `:videolib` | Direct `:app` consumer | Project dependency/source/package integration | `./gradlew :app:assembleDebug` | Validates additive public API compatibility and APK native merge through the confirmed Gradle edge | INT-03 |
| CHECK-04 | `:videolib` | Android runtime consumers | JNI loading, JVM callbacks, FFmpeg decode, worker/EGL lifecycle | `./gradlew :videolib:connectedDebugAndroidTest` once per available `arm64-v8a` and `armeabi-v7a` target, recording device/API/ABI | Only device execution proves native loading, thread attach/detach, callbacks, decoder behavior, and EGL lifecycle | INT-04 |
| CHECK-05 | `:videolib` | Host-surface consumers | Observable rendering/runtime behavior | Supported-device manual check using CH-13 through the public API: continuous frames, explicit stop, EOS/no-loop/final-frame retention, and later-play recovery; record device/API/ABI and evidence | Visible ordering and retained final pixels are not proven by compilation or callback-only assertions | INT-05 |

## 8. Shared infrastructure and risk constraints

| Risk | Affected Change / Task / AC | Required serialization or verification | Owning response task |
| --- | --- | --- | --- |
| Kotlin declaration/export mismatch or handle reinterpretation breaks at runtime | CH-01, CH-08; DEV-01/DEV-05; AC-9 | DEV-05 follows both language/native contracts; TEST-01 resolves every export; CHECK-02 inspects symbols; CHECK-04 loads them | DEV-05, TEST-01, INT-02/INT-04 |
| Stop, EOS, surface loss, error, and release race causes deadlock, UAF, or post-stop callback | CH-01, CH-06–CH-08; DEV-04/DEV-05; AC-2/AC-7 | Serialize DEV-03→DEV-04→DEV-05; never wait/join while holding a lock needed by worker/render/callback; instrumented race/lifecycle coverage and device execution | DEV-04, TEST-02/TEST-03, INT-04 |
| Borrowed AVFrame/RGBA storage is freed early or leaked | CH-06/CH-07; DEV-03; AC-8 | Decode worker solely owns FFmpeg and conversion objects; synchronous renderer handoff; every terminal/cancellation path covered on device | DEV-03, TEST-03, INT-04 |
| Decoder work blocks caller or GL executor; queue grows without bound | CH-06/CH-07; DEV-03; AC-1/AC-6 | Dedicated cancellable worker, bounded ownership, timestamp pacing, late-intermediate dropping; device timing/stop evidence | DEV-03, TEST-03, INT-04/INT-05 |
| Renderer silently reports success after EGL/swap failure | CH-04/CH-05; DEV-02; AC-4/AC-8 | Explicit synchronous presentation result before playback integration; surface-loss/error device coverage | DEV-02, TEST-03, INT-04 |
| Worker callback uses stale `JNIEnv*` or leaked/released Java reference | CH-01/CH-08; DEV-05; AC-4/AC-7 | `JNI_OnLoad` JVM cache, per-thread attach/detach, attempt ID, exact global/local ref ownership, no lock-held Java invocation; main-thread/at-most-once tests | DEV-05, TEST-02/TEST-03, INT-04 |
| Bundled format support is overstated | CH-03, CH-07, CH-13; AC-1/AC-4 | Public error stays capability-based; fixture proves only its own format; no fixed codec matrix in KDoc/tests | DEV-01/DEV-03, TEST-03 |
| ABI or 16 KB packaging regresses on one target | CH-09/CH-15; DEV-03; AC-9 | Keep current Gradle/CMake/archives untouched except source list; inspect both outputs and run each available ABI | INT-02, INT-04 |
| Existing preview or `NativeLib` consumers regress during native-handle migration | CH-08/CH-10; DEV-05; AC-9 | Preserve every existing export and behavior; binding regressions plus `:app` assembly | TEST-01, INT-03/INT-04 |
| Full local path leaks sensitive names into logs | CH-07/CH-08; DEV-03/DEV-05; AC-4 | Log semantic phase/native diagnostic only; source review in DEV tasks and device log check during failures | DEV-03/DEV-05, INT-04 |

Implementation handoff is ready: the DAG is acyclic, all ACs and consequential design invariants resolve to concrete production/test/integration ownership, and no blocker or protected operation remains in the planned scope.
