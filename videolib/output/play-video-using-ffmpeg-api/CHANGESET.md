AUTOMATION: CONTINUE

# CHANGESET — play-video-using-ffmpeg-api

## 1. Implementation outcome

| Item | Result |
| --- | --- |
| Outcome | Completed for the approved Android-dev range DEV-01–DEV-05. |
| Owning / changed module | `:videolib` only. |
| Consumer closure | `:app` remains the direct project consumer through `app/build.gradle.kts:55`; unknown external consumers remain subject to additive public/source/binary/runtime compatibility. |
| Reuse decision | Extended `VideoPreview`, `PreviewRenderer`, the existing render executor, GLES RGBA path, and bundled FFmpeg/CMake target. No parallel preview or GL abstraction and no DI framework were introduced. |
| Deviations | None from the approved behavior or change surface. Testing-owned CH-10–CH-14 and integration-owned CH-15 remain handed off. |
| Protected operations | No commit, push, publication, signing, upload, distribution, dependency change, FFmpeg rebuild, or vendored archive edit. |

## 2. Actual change manifest

| FR-ID | SC-ID | AC-ID | Work-ID / Story-ID | Task-ID | Change-ID | Owning module | Affected consumers / contracts | Design-Ref(s) | Planned action | Actual path | Actual symbol / resource / config key | Diff status | Purpose | Test-ID / Check-ID | Verification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FR-1–FR-4 | SC-1–SC-4 | AC-1–AC-7, AC-9 | W-0–W-4 / ST-1–ST-4 | DEV-01, DEV-05 | CH-01 | `:videolib` | Public Kotlin API, JNI callback/runtime behavior | none — requirement/code-driven | extend | `videolib/src/main/java/com/cii/videolib/VideoPreview.kt` | `VideoPreview.play`, `stop`, lifecycle ordering, `nativePlay`/`nativeStop`, attempt-aware `@Keep` native receivers and main-thread gate | completed | Add accepted/rejected playback, listener ownership, startup-race buffering, minification-safe JNI callbacks, stale event suppression, and cancellation barriers while preserving old methods | TEST-01–TEST-03; CHECK-01, CHECK-03, CHECK-04 | Source inspection; Kotlin compilation passed in scoped assemble. |
| FR-3, FR-4 | SC-3, SC-4 | AC-3, AC-4, AC-7 | W-0 / shared foundation | DEV-01 | CH-02 | `:videolib` | New public terminal contract | none — requirement/code-driven | create | `videolib/src/main/java/com/cii/videolib/PlaybackListener.kt` | `PlaybackListener.onPlaybackCompleted`, `onPlaybackError` | completed | Stable at-most-once completion/error callbacks | TEST-02, TEST-03; CHECK-01, CHECK-03 | Source inspection; Kotlin compilation passed. |
| FR-4 | SC-4 | AC-4 | W-0 / shared foundation | DEV-01 | CH-03 | `:videolib` | New public error contract | none — requirement/code-driven | create | `videolib/src/main/java/com/cii/videolib/PlaybackError.kt` | `PlaybackError.INPUT_OPEN`, `UNSUPPORTED_VIDEO`, `DECODE`, `RENDER` | completed | Prevent unstable FFmpeg/EGL codes from becoming public values | TEST-02, TEST-03; CHECK-01, CHECK-03 | Source inspection; Kotlin compilation passed. |
| FR-1, FR-4 | SC-1, SC-4 | AC-4, AC-8, AC-9 | W-0, W-1, W-4 | DEV-02 | CH-04 | `:videolib` | Native renderer contract | none — requirement/code-driven | extend | `videolib/src/main/cpp/preview_renderer.h` | `PreviewRenderer::pushFrame`, `isSurfaceReady` | completed | Make synchronous presentation success observable without changing renderer ownership | TEST-01, TEST-03; CHECK-01, CHECK-04 | Source inspection; both configured ABI native builds passed. |
| FR-1, FR-4 | SC-1, SC-4 | AC-4, AC-8, AC-9 | W-0, W-1, W-4 | DEV-02 | CH-05 | `:videolib` | EGL/GLES thread and presentation behavior | none — requirement/code-driven | extend | `videolib/src/main/cpp/preview_renderer.cpp` | `PreviewRenderer::pushFrame` | completed | Report make-current, GL, and swap failure; keep all GL calls on `videolib_render` | TEST-03; CHECK-01, CHECK-04, CHECK-05 | Source inspection; both configured ABI native builds passed. Device rendering not run. |
| FR-1–FR-4 | SC-1–SC-4 | AC-1–AC-8 | W-0–W-4 / ST-1–ST-4 | DEV-03, DEV-04 | CH-06 | `:videolib` | Native ownership/state/callback contract | none — requirement/code-driven | create | `videolib/src/main/cpp/video_playback.h` | `VideoPlayback`, `PlaybackState`, `PlaybackErrorCode`, `PlaybackTerminalCallback` | completed | Define one per-facade coordinator, renderer owner, attempt state, worker, cancellation, and terminal boundary | TEST-02, TEST-03; CHECK-01, CHECK-04 | Source inspection; C++ compilation/link passed on both configured ABIs. |
| FR-1–FR-4 | SC-1–SC-4 | AC-1–AC-8 | W-1–W-4 / ST-1–ST-4 | DEV-03, DEV-04 | CH-07 | `:videolib` | FFmpeg, threading, frame lifetime, renderer lifecycle | none — requirement/code-driven | create | `videolib/src/main/cpp/video_playback.cpp` | `VideoPlayback` lifecycle, `decodeAttempt`, `runAttempt`, `finishAttempt` | completed | Enforce readable local-file input; video-only FFmpeg open/select/decode/drain; monotonic timestamp pacing; worker-owned swscale/RGBA memory; synchronous presentation; idempotent stop; surface-loss/render failure teardown; exception containment | TEST-02, TEST-03; CHECK-01, CHECK-02, CHECK-04, CHECK-05 | Source/lock-order/ownership inspection; C++ compilation and static archive link passed for both ABIs. Runtime not run. |
| FR-1–FR-4 | SC-1–SC-4 | AC-1–AC-9 | W-0–W-4 / ST-1–ST-4 | DEV-05 | CH-08 | `:videolib` | Exact Kotlin/JNI signatures, JVM refs, legacy native API | none — requirement/code-driven | extend/wire | `videolib/src/main/cpp/videolib.cpp` | `JNI_OnLoad`, `PlaybackJniBridge`, `asPlayback`, all old exports, new `nativePlay`/`nativeStop` exports | completed | Migrate opaque handle to `VideoPlayback`, retain old delegates, copy/release input strings, attach/detach native threads, own global/local refs, and deliver attempt-aware outcomes without sensitive paths | TEST-01–TEST-03; CHECK-01–CHECK-04 | Paired declaration/export source audit; native compile/link passed. Device symbol resolution/callback execution handed off. |
| FR-1 | SC-1 | AC-1, AC-8, AC-9 | W-1 / ST-1 | DEV-03 | CH-09 | `:videolib` | CMake/native target and ABI build | none — requirement/code-driven | wire | `videolib/src/main/cpp/CMakeLists.txt` | `${CMAKE_PROJECT_NAME}` source list | completed | Add `video_playback.cpp` to existing `libvideolib.so`; leave link order, archives, ABI filters, toolchain, and alignment unchanged | CHECK-01, CHECK-02 | CMake configure/build/link passed for `arm64-v8a` and `armeabi-v7a`. |
| FR-1–FR-4 | SC-1–SC-4 | AC-5, AC-9 | W-0 | TEST-01 | CH-10 | `:videolib` | JNI/load/idempotence regressions | none — requirement/code-driven | extend | `videolib/src/androidTest/java/com/cii/videolib/PreviewBindingTest.kt` | existing and planned binding tests | unchanged — testing-owned | Preserve for testing-stage implementation | TEST-01; CHECK-01, CHECK-04 | Not edited or executed by Android-dev. |
| FR-2, FR-4 | SC-2, SC-4 | AC-2, AC-4, AC-5, AC-7, AC-9 | W-2, W-4 / ST-2, ST-4 | TEST-02 | CH-11 | `:videolib` | Public/JNI contract tests | none — requirement/code-driven | create | `videolib/src/androidTest/java/com/cii/videolib/PlaybackContractTest.kt` | planned test class | unchanged — testing-owned | Preserve for testing-stage implementation | TEST-02; CHECK-01, CHECK-04 | Not created or executed by Android-dev. |
| FR-1, FR-3, FR-4 | SC-1, SC-3, SC-4 | AC-1, AC-3, AC-4, AC-6–AC-9 | W-1, W-3, W-4 / ST-1, ST-3, ST-4 | TEST-03 | CH-12 | `:videolib` | Native/device lifecycle tests | none — requirement/code-driven | create | `videolib/src/androidTest/java/com/cii/videolib/PlaybackLifecycleTest.kt` | planned test class | unchanged — testing-owned | Preserve for testing-stage implementation | TEST-03; CHECK-01, CHECK-04, CHECK-05 | Not created or executed by Android-dev. |
| FR-1, FR-3 | SC-1, SC-3 | AC-1, AC-3, AC-6 | W-1, W-3 / ST-1, ST-3 | TEST-03 | CH-13 | `:videolib` test source | Deterministic device fixture | none — requirement/code-driven | create | `videolib/src/androidTest/assets/playback_short.mp4` | planned synthetic fixture | unchanged — testing-owned | Preserve for testing-stage implementation | TEST-03; CHECK-01, CHECK-04, CHECK-05 | Not created by Android-dev. |
| FR-1, FR-3 | SC-1, SC-3 | AC-1, AC-3, AC-6 | W-1, W-3 / ST-1, ST-3 | TEST-03 | CH-14 | `:videolib` test source | Fixture provenance/reproducibility | none — requirement/code-driven | create | `videolib/src/androidTest/assets/README.md` | planned fixture metadata | unchanged — testing-owned | Preserve for testing-stage implementation | TEST-03 | Not created by Android-dev. |
| FR-1–FR-4 | SC-1–SC-4 | AC-1–AC-9 | W-5 | INT-01–INT-05 | CH-15 | `:videolib` generated output | AAR/ELF/ABI consumers | none — requirement/code-driven | verify only | `videolib/build/outputs/aar/videolib-debug.aar`; ABI `libvideolib.so` intermediates | generated artifacts | unchanged — do not edit; regenerated by authorized build | Preserve authoritative source/build separation; package inspection remains integration-owned | CHECK-01–CHECK-05 | Debug AAR/native outputs generated by passing build; contents/alignment not inspected in this stage. |

## 3. Task completion

| Task-ID | Preconditions | Invariants checked | Done condition | Result | Evidence |
| --- | --- | --- | --- | --- | --- |
| DEV-01 | Approved public semantic contract and exact CH-01–CH-03 scope | Additive API; existing methods preserved; single-owner public calls; main-thread terminal delivery; no listener on rejection; attempt ID and pending-start race gate; stop/release stale-event suppression | Exact planned Kotlin symbols compile and event state cannot deliver a mismatched attempt | completed | Scoped source diff/audit and successful `:videolib:assembleDebug`. |
| DEV-02 | Existing synchronous renderer and single EGL executor | No FFmpeg in renderer; GL remains render-thread-only; buffer remains valid until synchronous return; current-context/GL/swap failures return false; full-surface RGBA path unchanged | Native caller receives reliable presentation result | completed | Caller/source inspection and both ABI CMake builds. |
| DEV-03 | DEV-02 renderer result seam; linked FFmpeg 7.1 archives | Local regular readable file only; video stream only; worker-owned format/codec/packet/frame/sws/RGBA; decoder drain; monotonic non-decreasing timing; no unbounded queue; synchronous handoff; allocation exceptions mapped to decode failure; no full-path logging | Supported input reaches internal completion/error with deterministic cleanup | completed | Source ownership review and passing C++ compile/static link on both configured ABIs. |
| DEV-04 | DEV-03 worker and attempt ownership | One state mutex arbitrates terminal winner; no state lock across renderer call, worker join, or callback; FFmpeg interrupt and condition wake on cancellation; detach joins before EGL teardown; release suppresses callback; render failure invalidates and tears down surface before notification | Stop/release/surface races have ordered, idempotent source paths and no post-return worker | completed | Lock/lifetime inspection and passing native builds. Device race execution handed off. |
| DEV-05 | DEV-01 exact Kotlin contract; DEV-03/DEV-04 complete native owner | Declaration/export names and `(J)V`/`(JI)V` callbacks match; callback receivers are `@Keep`; `JavaVM` cached; `JNIEnv*` thread-local; global/local/string refs released; old exports preserved; handle conversion consistent; callback invokes without lifecycle lock | Public play/stop and terminal outcomes are wired through JNI while legacy calls remain linkable and callback names survive consumer minification | completed | Paired source audit, refreshed graph for tracked symbols, and successful Kotlin/native assemble. New untracked graph nodes were validated by source/build because the incremental graph omitted them. |

## 4. Authorized command results

| Exact command | Scope | Outcome | Environment / notes |
| --- | --- | --- | --- |
| `git diff --check -- videolib/src/main && ./gradlew :videolib:assembleDebug` | Scoped source whitespace plus smallest library build | Build not started successfully: sandbox denied Gradle wrapper cache lock under the user Gradle directory | Source diff check completed first; no source effect. |
| `./gradlew :videolib:assembleDebug` | Smallest library compile/link | Failed during unrelated `:camera` configuration before `:videolib` tasks: plugin requires Java 21 but shell JVM was Java 17 | No source repair made; environment mismatch only. |
| `env JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" ./gradlew :videolib:assembleDebug` | Kotlin/JNI/CMake/FFmpeg/EGL debug build | PASS; 29 actionable tasks, 11 executed; both `arm64-v8a` and `armeabi-v7a` CMake build tasks completed | Java 21 Android Studio runtime; existing warnings for `:camera`/`:core` publication configuration and plugin Kotlin compatibility. |
| `JAVA_HOME=/Users/loannth20/.sdkman/candidates/java/21.0.9-tem ./gradlew :videolib:assembleDebug` | Attempted final rebuild | Failed before Gradle: invalid `JAVA_HOME` | No source/build effect. |
| `JAVA_HOME=/Library/Java/JavaVirtualMachines/temurin-21.jdk/Contents/Home ./gradlew :videolib:assembleDebug` | Attempted final rebuild | Failed before Gradle: invalid `JAVA_HOME` | No source/build effect. |
| `JAVA_HOME=/Applications/Android\ Studio.app/Contents/jbr/Contents/Home ./gradlew :videolib:assembleDebug` | Final scoped rebuild | PASS; 29 actionable tasks, 11 executed; both configured ABI native builds and Kotlin/AAR assembly completed | Java 21; rerun outside sandbox for Gradle cache access. Same pre-existing project warnings only. |

Testing commands were not run because TEST-01–TEST-03 are owned by the testing stage. `:app` assembly, AAR/ELF inspection, connected tests, and manual visual playback were not run because CHECK-02–CHECK-05 are integration-testing tasks.

Static/source evidence: all changed production files are within CH-01–CH-09; focused trailing-whitespace scan passes for those files; Kotlin/JNI method names and callback descriptors were paired manually; the CMake source addition preserves all existing link/ABI/alignment configuration. A repository-wide `git diff --check` still reports pre-existing trailing whitespace in `videolib/output/play-video-using-ffmpeg-api.md:3`, which is outside the approved implementation paths and was not modified here.

## 5. Testing handoff

| Testing Task-ID | Work-ID / Story-ID | AC-ID / risk | Test-ID | Level | Target component / contract | Behavior / transition / error scope | Fake / fixture boundary | Relevant changed paths / symbols | Depends on | Execution expectation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TEST-01 | W-0 | AC-5, AC-9; JNI symbol/handle regression | TEST-01 | Android instrumentation binding | `VideoPreview`, `NativeLib`, every old/new JNI export | Construct/load; no-surface rejection; push/pattern/detach guard; stop/release idempotence; legacy version/string; new play/stop resolution | No surface/media; direct buffer only for existing push contract | `VideoPreview`; `PlaybackListener`; `PlaybackError`; `videolib.cpp` exports and `asPlayback` | DEV-05 complete | Author in testing; compile via CHECK-01; execute on Android via CHECK-04. |
| TEST-02 | W-2 / ST-2, W-4 / ST-4 | AC-2, AC-4, AC-5, AC-7, AC-9; stale callback/terminal race | TEST-02 | Android instrumentation contract | Public facade/listener and JNI terminal bridge | Empty/no-surface rejection; accepted invalid/corrupt input category; overlap rejection; repeated stop; stop/release suppression; main-thread and at-most-once notification; relevant Idle/Starting/Playing/Stopping/Failed/Released transitions | Test-owned temporary paths/files; valid lightweight surface only for accepted attempts | `VideoPreview.play`/`stop`; private event gate; `PlaybackJniBridge`; `VideoPlayback` cancellation/finish | DEV-05 complete | Author in testing; compile via CHECK-01; device execution via CHECK-04. |
| TEST-03 | W-1 / ST-1, W-3 / ST-3, W-4 / ST-4 | AC-1, AC-3, AC-4, AC-6–AC-9; FFmpeg/frame/EGL/lifecycle | TEST-03 | Android instrumentation native integration | Bundled FFmpeg → `VideoPlayback` → `PreviewRenderer` → JNI terminal bridge | Supported local decode; timestamp-forward frames; delayed-frame drain; final presentation before one completion; stop and surface-loss winners; later-play recovery; no post-barrier event | Synthetic short H.264/MP4 fixture copied to app-private path; attached test surface; corrupt temporary input; document provenance/checksum | `video_playback.h/.cpp`; `PreviewRenderer::pushFrame`; `videolib.cpp`; public callback types | DEV-05 complete | Author in testing; compile via CHECK-01; each available ABI via CHECK-04; visible pixels via CHECK-05 only. |

## 6. Integration handoff

| Integration Task-ID | Check-ID | Changed module | Affected consumer / external contract | Boundary | Exact command or device/manual check | Required environment | Blocking policy |
| --- | --- | --- | --- | --- | --- | --- | --- |
| INT-01 | CHECK-01 | `:videolib` | Public/native library plus Android test sources | Kotlin/JNI/CMake/FFmpeg/EGL compile-link | `env JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" ./gradlew :videolib:assembleDebug :videolib:assembleDebugAndroidTest` | Java 21, Android SDK/NDK 29.0.14206865, CMake 3.22.1; testing artifacts CH-10–CH-14 present | Failure blocks device/integration handoff. Android-dev already proved `assembleDebug`; Android-test compilation remains. |
| INT-02 | CHECK-02 | `:videolib` | External AAR consumers | Native ABI/package/export/alignment | Inspect `videolib/build/outputs/aar/videolib-debug.aar` and both ABI `libvideolib.so` outputs with archive listing plus NDK `llvm-readelf`/`llvm-nm`: both configured ARM entries, legacy/new JNI exports, expected dynamic dependencies, 16 KB-compatible LOAD alignment | Successful CHECK-01 and NDK binutils | Missing ABI/export or incompatible alignment blocks continuation; never edit generated outputs. |
| INT-03 | CHECK-03 | `:videolib` | Direct `:app` consumer | Project source/package integration | `env JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" ./gradlew :app:assembleDebug` | Java 21 and Android toolchain | Failure blocks external-consumer confidence; do not modify app unless a separate approved repair changes scope. |
| INT-04 | CHECK-04 | `:videolib` | Android runtime consumers | JNI load/callback, FFmpeg decode, worker/EGL lifecycle | `env JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" ./gradlew :videolib:connectedDebugAndroidTest` once per available `arm64-v8a` and `armeabi-v7a` target; record device/API/ABI | CHECK-01/CHECK-02 pass; connected supported devices/emulators | Test failure blocks; unavailable ABI is recorded as environment-dependent, not success or source failure. |
| INT-05 | CHECK-05 | `:videolib` | Host-surface consumers | Observable GLES playback | Supported-device manual check with CH-13 via public API: continuous ordered frames, explicit stop, EOS/no-loop/final-frame retention while surface lives, and later-play recovery; record device/API/ABI and evidence | CHECK-04 pass and visible Surface host | Observable mismatch blocks; compilation/callback evidence cannot substitute. |

The module-impact closure remains `:videolib` production → `:app` compile/package consumer → unknown external consumers. No additional module, manifest, resource, dependency, DI, publication, or benchmark contract was introduced.
